// MIMETypes.js
// Exported mime types to use.

// Collection of mime types.
const mimeTypes = {
    "html": "text/html",
    "css": "text/css",
    "json": "application/json"
};

// Public export of codes.
module.exports = {
    mimeTypes
};